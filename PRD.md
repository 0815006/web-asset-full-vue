# 测试资产全景平台 - 产品需求文档 (PRD)

## 1. 项目概述
### 1.1 项目背景
随着测试资产（文档、脚本、导图等）数量的不断增加，传统的文件夹管理方式已无法满足快速检索、在线查阅和跨团队协作的需求。本项目旨在构建一个统一的测试资产管理平台，实现资产的结构化存储、全文检索及多格式在线预览。

### 1.2 项目目标
- 提供直观的资产全景视图，按专区和产品进行分类管理。
- 实现毫秒级的全文检索能力，支持文档内容搜索。
- 支持多种办公和技术文档的在线免下载预览。
- 提供完善的系统健康检查机制，确保物理存储与数据库、索引库的一致性。
- 支持完全离线的内网部署环境。

---

## 2. 用户角色定义
| 角色 | 描述 |
| :--- | :--- |
| **普通用户** | 浏览资产全景，搜索并查阅/下载相关文档。 |
| **测试经理** | 管理所属产品的资产，执行文件上传、更新和目录维护。 |
| **系统管理员** | 维护系统运行，执行存储和索引的健康检查及修复。 |

---

## 3. 功能需求

### 3.1 资产全景首页
- **全局搜索**：支持对全库资产的名称和内容进行关键词检索。
- **专区导航**：
    - **测试技术及工艺专区**：存放通用的技术规范、工艺流程。
    - **测试管理专区**：存放管理制度、模板、计划。
    - **产品专区**：按团队和业务领域展示各个产品的私有资产。
- **布局切换**：支持“瀑布流”和“标签页”两种视图模式。

### 3.2 产品详情页
- **画像展示**：显示产品名称、负责人、所属团队、资产总数等元数据。
- **局部搜索**：支持在当前产品范围内进行精准搜索。
- **资产树管理**：
    - 采用懒加载技术展示无限级目录结构。
    - 支持新建文件夹、重命名、移动、逻辑删除。
- **批量操作**：支持勾选多个文件/文件夹进行一键打包下载（ZIP 格式）。

### 3.3 超级预览弹窗
- **多格式支持**：
    - **PDF**：原生沉浸式预览，支持缩放、打印。
    - **Office (Word/Excel/PPT)**：集成 OnlyOffice 引擎，高度还原排版，支持只读模式。
    - **思维导图 (XMind)**：纯前端解析渲染，支持交互式缩放拖拽。
    - **图片**：支持常见格式的大图查看。
    - **代码/文本**：支持 28 种文本格式（Java, Python, SQL, JSON, YAML 等）的语法高亮预览。
    - **Markdown**：支持 `.md` 文件的实时渲染展示。
- **版本更新**：在预览界面可直接上传新文件覆盖当前版本，系统自动递增版本号并更新索引。
- **单文件下载**：无需打包，直接下载当前查看的原始文件。

### 3.4 搜索引擎
- **全文检索**：基于 Apache Solr 实现，支持关键词高亮和相关度排序。
- **自动降级**：当 Solr 服务不可用时，系统自动切换至数据库 `LIKE` 模糊搜索，确保核心功能不中断。
- **区域识别**：搜索结果清晰标注所属专区或具体产品名称。

### 3.5 系统健康检查
- **存储健康检查**：自动比对物理磁盘文件与数据库记录，识别“文件丢失”或“冗余文件”，支持一键入库。
- **索引健康检查**：比对数据库记录与 Solr 索引状态，支持一键补全缺失索引或清理无效索引。

---

## 4. 技术架构
- **前端**：Vue 2.7 + Element UI + Axios + Vue CLI。
- **后端**：Spring Boot 2.7 + MyBatis Plus + Maven。
- **存储**：本地物理存储 + 数据库物化路径（`tree_path`）。
- **检索**：Apache Solr + Apache Tika (文档解析)。
- **预览**：OnlyOffice Document Server (Docker) + XMindViewer (JS)。

---

## 5. 非功能需求
- **内网支持**：所有第三方 SDK（Element UI, XMindViewer 等）均已本地化，支持无外网环境部署。
- **性能要求**：目录树支持万级节点懒加载，搜索响应时间 < 500ms。
- **安全性**：预览界面强制只读，支持动态水印（规划中），防止敏感数据泄露。
